[project]
name = "household-chores"
version = "0.1.0"
description = "A family web application for managing shared household chores."
requires-python = ">=3.10"
dependencies = [
    "django>=5.0",
]

[tool.uv]
package = false
